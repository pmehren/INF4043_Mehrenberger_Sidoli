package fr.esiea.binome.Mehrenberger_Sidoli.app;

//import java.util.Arrays;
//import java.util.List;

//import fr.esiea.unique.binome.name.bag.SmartBag;
//import fr.esiea.unique.binome.name.lettergameengine.LetterGameEngine;
//import fr.esiea.unique.binome.name.player.Player;

public class App{
	public static void main( String[] args )
	{
		/*
		Player p1 = new Player();
		Player p2 = new Player();
		List<Player> players = Arrays.asList(p1,p2);
		for (Player player : players) {
			player.setLetterBag(bag);
		}
		LetterGameEngine letterGameEngine = new LetterGameEngine(players, new SmartBag());
		letterGameEngine.run();
	*/
	}	
}
